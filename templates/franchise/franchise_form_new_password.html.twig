{% set use_nav = false %}

{% extends 'base.html.twig' %}

{% block body %}
    <article class="container">
        <h2 class="text-center my-4">Choisissez votre mot de passe :</h2>
        {{ form_start(franchisePasswordNew) }}
        <div class="d-flex flex-column justify-content-center">
            {{ form_widget(franchisePasswordNew.plainPassword) }}
            {{ form_widget(franchisePasswordNew.plainPassword, {'attr': {'class' : 'rounded-3 my-2'}}) }}
        </div>
        <div>
            <button class="btn btn-success">
                Valider
            </button>
        </div>
        {{ form_end(franchisePasswordNew) }}
    </article>
{% endblock %}