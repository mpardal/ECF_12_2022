{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(franchiseRegistrationForm, {'attr': {'class': 'champs'}}) }}
        {{ form_row(franchiseRegistrationForm.email, {
            label: 'Identifiant (email) :'
        }) }}
        {{ form_row(franchiseRegistrationForm.password, {
            label: 'Mot de passe :'
        }) }}
    {#
    {{ form_row(registrationForm.agreeTerms) }}
    #}

        <button type="submit" class="btn">Inscrire</button>
    {{ form_end(franchiseRegistrationForm) }}
{% endblock %}
