{%  if structure.imageName %}
    <img src="{{ vich_uploader_asset(structure, 'imageFile') }}" alt="image" class="img-fluid">

{% else %}
    <img src="{{ '/images/salle.jpeg' }}" alt="image" class="img-fluid">

{% endif %}