{% set use_nav = false %}

{% extends 'base.html.twig' %}

{% block body %}
    <article class="container">
        {{ form_start(structurePasswordNew) }}
        <div>
            {{ form_widget(structurePasswordNew.plainPassword) }}
        </div>
        <div></div>
        {{ form_end(structurePasswordNew) }}
    </article>
{% endblock %}
